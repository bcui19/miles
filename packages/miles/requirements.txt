accelerate
blobfile
datasets
httpx[http2]
hypothesis
mcp[cli]
memray  # needed for debugging (but is lightweight), we can put it to dev mode when using pyproject.toml
omegaconf
pillow
polars
pybase64
pylatexenc
pytest-asyncio
pyyaml
qwen_vl_utils # for VLM
ray[default]
ring_flash_attn; platform_system == "Linux"
sglang-router>=0.2.3
tensorboard
torchft-nightly==2026.4.3; platform_system == "Linux"
transformers==5.10.2  # pinned: 5.11+ adds native deepseek_v32 support, which trips miles' register_hf_config_aliases guard
wandb
